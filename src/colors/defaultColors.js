import { blueGrey, grey } from "@material-ui/core/colors";

const defaultColors = {
  // AppBackgroundColor: blueGrey[50],
  CardBackgroundColor: blueGrey[50],
  FontColor: grey[800],
};

export default defaultColors;
